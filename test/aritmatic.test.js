import { describe } from "vitest";

describe("pertambahan", () => {
    test(" hasil harus sesuai operasi dan input harus sesuai dengan type data int atau float", () => {
        const result = pertambahan(2, 3);
    });
});